// The encyclopedia is a client-only interactive page (canvas, matchMedia,
// live color math). Match the rest of the SPA: no SSR, no prerender.
export const prerender = false;
export const ssr = false;
